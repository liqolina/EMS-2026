#ifndef ENVIRONMENT_ENV_H
#define ENVIRONMENT_ENV_H


/*
  =====================================================
  KONFIGURASI WIFI STATION / CLIENT
  =====================================================
*/

#define wifiRunMode         WIFI_STA_ONLY

#define STA_SSID            "Berbayar"     
#define STA_PASSWORD        "seribu1gb"  
#define DEVICE_HOSTNAME     "ESP32-SENSOR-CLIENT-DC-1"    

#define USE_STATIC_IP       false

#define STA_LOCAL_IP        IPAddress(192, 168, 1, 50)
#define STA_GATEWAY         IPAddress(192, 168, 1, 1)
#define STA_SUBNET          IPAddress(255, 255, 255, 0)
#define STA_DNS1            IPAddress(1, 1, 1, 1)
#define STA_DNS2            IPAddress(8, 8, 8, 8)

/*
  =====================================================
  KONFIGURASI ACCESS POINT / HOTSPOT
  =====================================================
*/

#define AP_SSID             "ESP32_GATEWAY_NET"     
#define AP_PASSWORD         "12345678"  

#define AP_LOCAL_IP         IPAddress(10, 10, 10, 1)
#define AP_GATEWAY          IPAddress(10, 10, 10, 1)
#define AP_SUBNET           IPAddress(255, 255, 255, 0)
#define AP_CHANNEL          6
#define AP_MAX_CONNECTION   4
#define AP_HIDDEN           false

/*
  =====================================================
  INFORMASI ESP32
  =====================================================
*/

// DDMMYYMMLLSSSS: Category | Model | Year | Month | Batch | Serial
#define DEFAULT_ID_ESP      "10102606010002"

#define DEFAULT_NAME_ESP    "ESP32-SENSOR-CLIENT-DC-1"
#define DEFAULT_LOC_ESP     "BANDUNG"

/*
  =====================================================
  GATEWAY ADDRESS
  =====================================================
*/

#define GATEWAY_PEER_ADDRESS { 0x3C, 0x0F, 0x02, 0xD2, 0x1E, 0x20 }


/*
  =====================================================
  OTA ESP32
  =====================================================
*/

#define GITHUB_OWNER                     "liqolina"
#define GITHUB_REPO                      "EMS-2026"
#define FIRMWARE_ASSET_NAME              "firmware.bin"

// Kosongkan untuk repo publik.
// Untuk repo privat, isi token dengan permission Contents: read.
#define GITHUB_TOKEN                     "token-github"

#define GITHUB_API_VERSION               "2026-03-10"

#define CHECK_ON_BOOT                    true
#define AUTO_REBOOT_AFTER_UPDATE         true
#define ENABLE_ROLLBACK_CONFIRM          true

#define CHECK_INTERVAL_MS                (5UL * 60UL * 1000UL)   // 5 menit
#define HTTP_CONNECT_TIMEOUT_MS          15000UL
#define HTTP_READ_TIMEOUT_MS             30000UL

#define MAX_REDIRECTS                    5
#define JSON_DOC_CAPACITY                (24 * 1024)
#define DOWNLOAD_BUFFER_SIZE             4096

// MODE STRICT_TLS
// TESTING_INSECURE_TLS,
// AUTO_CA_CERT_BUNDLE,
// MANUAL_CA_CERT_BUNDLE,
// MANUAL_CA_CERT

#define STRICT_TLS                       TESTING_INSECURE_TLS
#define ALLOW_INSECURE_TLS_FOR_TESTING   false

// Sectigo Root E46
static const char GITHUB_API_CA[] PROGMEM = R"EOF()EOF";

//Sectigo Root E46
#define GITHUB_WEB_CA GITHUB_API_CA
#define GITHUB_ASSET_CA GITHUB_API_CA

// Let’s Encrypt chain
// static const char GITHUB_ASSET_CA[] PROGMEM = R"EOF()EOF";

// Isi CA PEM untuk produksi dalam bentuk file di littleFS.
// #define GITHUB_API_CA "/DigiCertTLSRSA4096RootG5.crt.pem"
// #define GITHUB_WEB_CA "/DigiCertTLSRSA4096RootG5.crt.pem"
// #define GITHUB_ASSET_CA "/DigiCertTLSRSA4096RootG5.crt.pem"


#endif
